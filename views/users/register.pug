extends ../layout

block content

    div.content
        div.form-header Register
        div
            form(action="/register" method="POST")
                div.input-form-row
                    div.input-container
                        label(for="first_name")